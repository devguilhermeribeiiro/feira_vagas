<div id="<%= dom_id job %>">
  <div>
    <p><%= job.employer %></p>
    <p><%= job.created_at.strftime('%B %d, %Y') %></p>
  </div>
  
  <div>
    <h2><%= job.title %></h2>
  </div>

  <div>
    <p><%= job.location %></p>
    <p><%= job.mode %></p>
  </div>
</div>